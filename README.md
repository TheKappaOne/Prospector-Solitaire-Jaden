# Prospector-Solitaire-Jaden
This has the first part of the Prospector chapter implemented. It creates a standard deck of 52 playing cards. The cards are implemented as a collection of Sprite renderers on a higher layer than a single rectangular image that acts as the face of the card.
